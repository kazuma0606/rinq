[package]
name = "rinq-syntax"
version = "4.0.0"
edition = "2024"
description = "LINQ-style `query!` macro for the rinq query engine (experimental)"
license = "MIT"
repository = "https://github.com/yoshidev/rusted-ca"
keywords = ["query", "linq", "macro", "rinq"]
categories = ["rust-patterns"]
readme = "README.md"

[lib]
proc-macro = true

[dependencies]
syn  = { version = "2", features = ["full"] }
quote = "1"
proc-macro2 = "1"

[dev-dependencies]
rinq = { path = ".." }

[package.metadata.docs.rs]
all-features = true
